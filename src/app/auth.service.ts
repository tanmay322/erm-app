import { Injectable } from '@angular/core';

@Injectable(
  // providedIn: 'root'
)
export class AuthService {

  constructor() { }
  getUserDetails(){
    //post details to the Api server and then returning the info if valid
  }
}
